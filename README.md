A tool to shift the UIDs/GIDs of a file hierarchy, with support for ACLs.

Useful to migrate LXC containers from privileged to unprivileged (using the subuid/subgid offset for the user running the container) and back.
